[build-system]
requires = ["setuptools==68.2.2", "wheel", "torch==2.0.0"]
build-backend = "setuptools.build_meta:__legacy__"

dependencies = [
    "natsort",
    "psutil",
    "timeout-decorator",
    "pyrender==0.1.45",
    "omegaconf",
    "hydra-core>=1.0.5",
    "pandas==1.4.1",
    "opencv-python",
]